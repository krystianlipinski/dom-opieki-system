services:
  db:
    image: postgres:14-alpine
    container_name: domopieki_db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: domopieki_backend
    depends_on:
      - db
    dns: # <-- DODANA SEKCJA DNS
      - 8.8.8.8
      - 1.1.1.1
    ports:
      - "3000:3000"
    volumes:
      # Montujemy kod, ale izolujemy node_modules
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - .env
    # command: tail -f /dev/null

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: domopieki_frontend
    ports:
      - "5173:5173"
    volumes:
      # Montujemy kod, ale izolujemy node_modules
      - ./frontend:/app
      - /app/node_modules
    env_file:
      - .env

volumes:
  postgres_data: